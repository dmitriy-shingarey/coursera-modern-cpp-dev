# Calculate product price after discount  

  Inputs:
      N, A, B, X, Y (A < B)
      N - initial price
      X - discount if X > A
      Y - discount if X > B
  Outputs:
      price after discount*/
